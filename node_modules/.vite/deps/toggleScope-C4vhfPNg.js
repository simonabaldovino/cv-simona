import { Mn as effectScope, Vn as onScopeDispose, gn as watch } from "./vue.runtime.esm-bundler-C6sZGmPl.js";
//#region node_modules/vuetify/lib/composables/toggleScope.js
function useToggleScope(source, fn) {
	let scope;
	function start() {
		scope = effectScope();
		scope.run(() => fn.length ? fn(() => {
			scope?.stop();
			start();
		}) : fn());
	}
	watch(source, (active) => {
		if (active && !scope) start();
		else if (!active) {
			scope?.stop();
			scope = void 0;
		}
	}, { immediate: true });
	onScopeDispose(() => {
		scope?.stop();
	});
}
//#endregion
export { useToggleScope as t };

//# sourceMappingURL=toggleScope-C4vhfPNg.js.map